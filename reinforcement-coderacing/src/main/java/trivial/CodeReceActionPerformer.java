package trivial;

import abstractions.ActionPerformer;
import model.Car;
import model.Game;
import model.Move;
import model.World;

public class CodeReceActionPerformer implements ActionPerformer<CodeRacingAction> {
    @Override
    public void perform(Car self, World world, Game game, Move move, CodeRacingAction action) {

    }
}
